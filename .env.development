# 开发环境配置
NODE_ENV=development

# API 基础路径 - 开发环境可以选择使用代理或直接连接
# 使用代理时（推荐）：/api
# 直接连接时：http://localhost:3000/api
VITE_API_BASE_URL=/api

# 后端服务地址（用于代理配置）
VITE_BACKEND_URL=http://localhost:3000

# 应用标题
VITE_APP_TITLE=移动端管理后台(开发环境)

# 是否启用 mock 数据（设为 false 使用真实后端）
VITE_USE_MOCK=false

# 是否启用调试工具
VITE_USE_DEBUGGER=true

# 是否在控制台显示请求日志
VITE_CONSOLE_REQUEST_LOG=true

# API 请求超时时间（毫秒）
VITE_API_TIMEOUT=10000